# Getting Set Up

To begin developing on the reqfire frontend clone the repo and run the following commands in a terminal with npm and node >= 4.00 installed.

```cd frontend```

```npm install```

```npm run dev```

Your browser should open to `localhost:3000` which will then redirect to `/app/`
