import React, { Component } from 'react';
import { observer } from 'mobx-react';

@observer
class ObjectEditorRelationship extends Component {
	static propTypes = {
		
	}

	render() {
		return (
			<div><h3>ObjectEditorRelationship</h3></div>
		);
	}
}

export default ObjectEditorRelationship;
